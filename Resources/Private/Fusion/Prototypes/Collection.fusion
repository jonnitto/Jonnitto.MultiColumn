// Namespaces are defined in "Root.fusion"

prototype(Jonnitto.MultiColumn:Collection) < prototype(Neos.Fusion:Collection) {
    collection = ${q(node).children('[instanceof Neos.Neos:ContentCollection]')}
    itemName = 'node'
    iterationName = 'iteration'
    itemRenderer = Jonnitto.MultiColumn:ContentCollection
}
