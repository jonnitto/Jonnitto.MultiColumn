prototype(MC:Collection) < prototype(Fusion:Collection) {
	collection = ${q(node).children('[instanceof TYPO3.Neos:ContentCollection]')}
	itemName = 'node'
	iterationName = 'iteration'
	itemRenderer = MC:ContentCollection
}
